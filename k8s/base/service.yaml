apiVersion: v1
kind: Service
metadata:
  name: aframp-backend
  labels:
    app: aframp-backend
spec:
  selector:
    app: aframp-backend
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
